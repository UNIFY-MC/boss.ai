# Story 1.3: Mover hooks para `settings.json` commitado

**Epic:** epic-001-tech-debt-remediation
**Status:** Draft
**Wave:** 1
**TD source:** TD-001
**Severity:** HIGH
**Effort:** S (30 min)

## Goal

Mover o registo dos 2 hooks `.cjs` de governança (`enforce-git-push-authority.cjs`, `synapse-engine.cjs`) de `.claude/settings.local.json` (gitignored) para `.claude/settings.json` (commitado). Restaura o Article II — Agent Authority em clones novos, em vez de depender de re-instalação manual.

## Acceptance Criteria

- [ ] AC1: `.claude/settings.json` contém os 2 hooks `.cjs` registados em `PreToolUse` (e `PostToolUse` se aplicável) — versionados no repo
- [ ] AC2: `.claude/settings.local.json` deixa de declarar esses hooks (continua para overrides locais legítimos)
- [ ] AC3: Clone fresh do repo + abertura no Claude Code = hooks activos imediatamente (sem passos manuais)
- [ ] AC4: `git diff` não revela credenciais nem paths privados em `settings.json`
- [ ] AC5: Smoke test — tentar `git push` como `@dev` resulta em block (hook funcional)

## Tasks

- [ ] Task 1: Ler `.claude/settings.local.json` actual e identificar bloco `hooks`
- [ ] Task 2: Copiar bloco `hooks` (PreToolUse + PostToolUse + ...) para `.claude/settings.json`
- [ ] Task 3: Remover bloco `hooks` de `.claude/settings.local.json` (manter o ficheiro para overrides futuros)
- [ ] Task 4: Verificar `.gitignore` confirma `settings.local.json` ignorado mas `settings.json` versionado
- [ ] Task 5: Smoke test: simular activação de hook (PreToolUse de Bash com `git push`) e verificar block
- [ ] Task 6: Documentar a decisão em comentário no topo do `settings.json` (opcional mas recomendado)

## Out of scope

- Adicionar novos hooks
- Refactor dos `.cjs` em si (Story 3.4 cobre testes; refactor é separado)
- Documentação completa em `.claude/hooks/README.md` (Story 5.1)

## File list

(vazio — preenchido por @dev durante implementação)

## Dependencies

- Blocks: 5.1 (READMEs devem reflectir o estado final após esta migração)
- Blocked by: nenhum
