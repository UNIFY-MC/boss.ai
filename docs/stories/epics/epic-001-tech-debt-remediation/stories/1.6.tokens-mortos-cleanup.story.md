# Story 1.6: Remover tokens CSS "gold/cream" mortos

**Epic:** epic-001-tech-debt-remediation
**Status:** Draft
**Wave:** 1
**TD source:** TD-013
**Severity:** LOW (mas estratégico — pré-requisito limpo para Story 2.1)
**Effort:** XS (5 min)

## Goal

Remover tokens "gold" e "cream" mortos (não referenciados em nenhum lado) do `:root` em `aiox-cohort-fundamentals/` e `reprise-masterclass-design-ia/`. Limpa o CSS e evita arrastar lixo para o refactor do design system (Wave 2 Story 2.1).

## Acceptance Criteria

- [ ] AC1: `grep -r 'var(--gold' acervo-formacoes/` retorna 0 (ou só nas 2 formações que de facto usam)
- [ ] AC2: `grep -r 'var(--cream' acervo-formacoes/` retorna 0 (ou só nas 2 formações que de facto usam)
- [ ] AC3: Tokens `--gold-*` e `--cream-*` removidos do `:root` em ficheiros das formações onde **não** são consumidos
- [ ] AC4: Páginas das 2 formações continuam visualmente idênticas (smoke test browser)

## Tasks

- [ ] Task 1: Em cada formação, grep dos tokens "gold" e "cream" — identificar quais NÃO são consumidos
- [ ] Task 2: Em cada `:root` afectado, remover as declarações dos tokens não consumidos
- [ ] Task 3: Smoke test visual de ambas as formações

## Out of scope

- Refactor do design system completo (Story 2.1)
- Renaming de outros tokens que estejam a ser usados

## File list

(vazio — preenchido por @dev durante implementação)

## Dependencies

- Blocks: 2.1 (deve ser feita ANTES do refactor de tokens para evitar arrastar lixo)
- Blocked by: nenhum
